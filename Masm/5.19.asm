DATAS SEGMENT
    ;此处输入数据段代码
    ARRAY1 DB 2,3,4,6,8,11,15,22,34,41,45,49,54,62,65
    ARRAY2 DB 2,3,5,6,7,11,12,14,34,44,45,49,54,62,65,66,67,68,69,70
    ARRAY3 DB 15 DUP(0),'$'
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
    DB 8 DUP(0)
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    MOV CX,15
    MOV BX,0
S:	MOV AX,[BX]
	PUSH CX
	MOV DI,0
	MOV CX,20
S1:	MOV SI,0
	CMP AX,[SI+15]
	JNZ NEXT
	MOV [DI+35],AX
NEXT:
	INC SI
	LOOP S1
	POP CX
	INC BX
	LOOP S
	
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START

